apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "anna-wordpress.fullname" . }}-test-connection"
  labels:
    {{- include "anna-wordpress.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "anna-wordpress.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
